package exemplo_interfaces.equipamentos.impressora;

// "interface" declara a o elemento como interface
public interface Impressora {
    // o método numa interface funciona de forma semelhante ao abstract mesmo não sendo declarado como
    public void imprimir();
}
