package exemplo_interfaces.equipamentos.digitalizadora;

// "interface" declara a o elemento como interface
public interface Digitalizadora {
    // o método numa interface funciona de forma semelhante ao abstract mesmo não
    // sendo declarado como
    public void digitalizar();
}
